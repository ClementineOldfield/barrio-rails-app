<h2>New message to <%= @recipient.first_name.capitalize %></h2>

<%= form_with model: @message, local: true do |f| %>
  <div class="field">
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit "Send"  %>
  </div>
<% end %>